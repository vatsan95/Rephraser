{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main app (tray + plugins).",
  "windows": ["main", "panel"],
  "permissions": [
    "core:default",
    "log:default",
    "notification:default",
    "dialog:default",
    "store:default"
  ]
}
